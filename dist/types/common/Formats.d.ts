export type Format = "array" | "csv" | "html" | "json" | "json-list" | "markdown" | "table" | "text" | "xml" | "yaml";
export declare enum Formats {
    ARRAY = "array",
    CSV = "csv",
    HTML = "html",
    JSON = "json",
    JSONLIST = "json-list",
    MARKDOWN = "markdown",
    TABLE = "table",
    TEXT = "text",
    XML = "xml",
    YAML = "yaml"
}
